# 开发日志 - 2026-05-26

## 已完成

- [x] **编辑/只读模式切换**：
  - EditNotePage 顶部新增切换按钮（编辑中 / 只读）
  - 只读模式：标题渲染为 h1、摘录编辑器只读（工具栏隐藏）、照片静态展示、标签和文件夹选择器隐藏
  - 编辑模式：与原来一致，所有控件可用
  - RichTextEditor 组件新增 `readonly` prop，通过 TipTap 的 `setEditable()` 控制

- [x] **文件附件（PDF/Word）**：
  - 新增 `Attachment` 类型（id, name, type, size, data)
  - Note 加 `attachments` 字段
  - `AttachmentPicker` 组件：支持点击/拖拽上传 PDF/Word，显示文件名+大小，悬停删除
  - 新建和编辑页均可添加/删除附件
  - 只读模式下附件以静态列表展示
  - 超过 10MB 标注「大文件」提示

- [x] **导出/导入笔记**：
  - `src/lib/transfer.ts`：exportAll、exportFolder、importData 三函数
  - 导出为 `.klog` 文件（JSON 格式，含文件夹+笔记+照片+附件）
  - 导入时自动检测 ID 冲突，提示用户确认后跳过重复项
  - 全部笔记页：「导出」「导入」按钮
  - 文件夹内页：「导出」按钮（导出当前文件夹及其子内容）
  - 附件可点击用浏览器打开

- [x] **图片拖拽排序 + 页码标注**：
  - ImagePicker 支持拖拽换位
  - 每张照片左下角自动标序号 1, 2, 3...
  - 拖拽后序号自动更新

- [x] **布局调整**：笔记页顺序改为 标题 → 摘录 → 照片 → 附件 → 标签 → 文件夹

- [x] **附件可点击打开**：点击附件用浏览器内置阅读器查看

- [x] **品牌改名 NoteSnap → Klog**：
  - 导航栏渐变 Logo：深蓝 K → 浅蓝 g，等宽字体加粗
  - 导出文件后缀 .klog
  - 数据库名 KlogDB
  - 页面标题、文档全量更新

- [x] **GitHub Pages 部署**：
  - 代码推送至 github.com/lumos0219-cyber/notesapp
  - gh-pages 分支自动部署，HTTPS 方式访问
  - HashRouter 适配静态托管
  - PWA Service Worker 移除（缓存问题影响更新）

- [ ] **未保存退出提示**：多次尝试未成功，已回滚。方案问题在于 TipTap 编辑器的 onChange 与初始内容同步难以区分。后续可重新考虑。

## 当前状态

- 线上地址：https://lumos0219-cyber.github.io/notesapp/
- 核心功能稳定：文件夹嵌套、多图排序、富文本摘录、标签筛选、附件上传、导出导入、编辑/只读切换
- OCR 模块保留但未使用（Tesseract.js、PaddleOCR、Google Vision 三套引擎代码均在）
