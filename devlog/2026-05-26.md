# 开发日志 - 2026-05-26

## 已完成

- [x] **编辑/只读模式切换**：
  - EditNotePage 顶部新增切换按钮（编辑中 / 只读）
  - 只读模式：标题渲染为 h1、摘录编辑器只读（工具栏隐藏）、照片静态展示、标签和文件夹选择器隐藏
  - 编辑模式：与原来一致，所有控件可用
  - RichTextEditor 组件新增 `readonly` prop，通过 TipTap 的 `setEditable()` 控制

- [x] **文件附件（PDF/Word）**：
  - 新增 `Attachment` 类型（id, name, type, size, data)
  - Note 加 `attachments` 字段
  - `AttachmentPicker` 组件：支持点击/拖拽上传 PDF/Word，显示文件名+大小，悬停删除
  - 新建和编辑页均可添加/删除附件
  - 只读模式下附件以静态列表展示
  - 超过 10MB 标注「大文件」提示

- [x] **导出/导入笔记**：
  - `src/lib/transfer.ts`：exportAll、exportFolder、importData 三函数
  - 导出为 `.notesnap` 文件（JSON 格式，含文件夹+笔记+照片+附件）
  - 导入时自动检测 ID 冲突，提示用户确认后跳过重复项
  - 全部笔记页：「导出」「导入」按钮
  - 文件夹内页：「导出」按钮（导出当前文件夹及其子内容）
  - 附件可点击用浏览器打开
