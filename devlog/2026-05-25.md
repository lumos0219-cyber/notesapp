# 开发日志 - 2026-05-25

## 已完成

- [x] 项目初始化：Vite + React 18 + TypeScript + Tailwind CSS 4
- [x] 三页面路由搭建：笔记列表 `/`、新建笔记 `/notes/new`、编辑笔记 `/notes/:id/edit`
- [x] 核心组件：Layout、EmptyState、NoteCard、UploadZone、OCRProgressBar、RichTextEditor
- [x] Dexie.js IndexedDB 本地存储（notes + corrections 两张表）
- [x] Tesseract.js OCR 集成（chi_sim+eng 中英文识别）
- [x] 用户修正词库学习机制（保存时对比原始 vs 修改，存入 corrections 表）
- [x] PWA 配置：Service Worker + Manifest + 桌面安装
- [x] 项目文档：docs/ 下创建需求、技术栈、设计规范、开发流程标准文件
- [x] 项目本地 devlog 系统建立
- [x] CLAUDE.md AI 助手指引文件

## 下午更新 (第二轮)

- [x] **修复 OCR 字母拆分 Bug**：splitMergedWord 算法误将单字母作为有效词条，移除 `candidate.length === 1` 条件，单词不再被拆成字母
- [x] **深度调试数据持久化**：
  - db.ts 新增 `debugDump()` 方法，导出完整 DB 状态
  - NewNotePage 写入后立即回读验证（write verification）
  - NoteListPage 加载时输出完整 DB 快照到控制台
  - 列表页底部新增「调试信息」折叠面板，不打开控制台也能看 DB 状态

## 晚间更新 (第三轮)

- [x] **修复根本 Bug：列表页数据不显示**：DB schema v1 缺少 `updatedAt` 索引，`orderBy('updatedAt')` 抛 `DexieError`，升级到 v2 schema 添加索引
- [x] **修复 TipTap 警告**：StarterKit 与显式 `ListItem` 重复注册，添加 `listItem: false` 配置
- [x] **云端手写 OCR 引擎**：
  - 新建 `src/lib/ocr-cloud.ts`，封装 Google Cloud Vision API (DOCUMENT_TEXT_DETECTION)
  - API Key 存储在 localStorage，页面上可设置/移除
  - 上传页新增引擎切换：本地引擎（Tesseract，打印体免费） / 云端引擎（Google Vision，手写体）
  - 选择云端引擎时提示设置 API Key

## 夜间更新 (第四轮)

- [x] **PaddleOCR 本地手写识别引擎**：
  - Python Flask 服务监听 `localhost:8765`
  - 使用 PIL + numpy 加载图片传给 PaddleOCR 3.x，绕过临时文件 I/O 问题
  - 前端新增「Paddle OCR」引擎为默认选项（Third engine）
  - 端到端验证通过：图片 → base64 → 本地服务 → 返回识别文字
  - 首次启动下载模型 ~200MB，之后秒开
  - 启动命令：`server/start.sh` 或 `server/venv/bin/python server/ocr_server.py`

## 当前状态

- **Paddle OCR**（默认）：手写+打印体，本地 Python 服务，免费
- **浏览器引擎**：打印体，Tesseract.js 浏览器内运行，免费
- **云端引擎**：手写体，Google Vision API，需 API 密钥（保留备用）
- 列表页数据持久化：已修复（DB v2 schema 添加 updatedAt 索引）
